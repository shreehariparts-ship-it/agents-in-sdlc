{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.78", "--executable-path", "/opt/pw-browsers/chromium", "--headless", "--no-sandbox"],
      "tools": ["*"]
    }
  }
}